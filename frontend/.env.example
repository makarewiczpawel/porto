# Must be the same registrable domain as the frontend, or the browser will not
# keep the refresh cookie. Locally that means matching the host exactly:
# if the app runs on 127.0.0.1, the API must be 127.0.0.1 too — not localhost.
VITE_API_URL=http://localhost:8000
